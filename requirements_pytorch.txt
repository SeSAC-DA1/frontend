# CarFinanceAI - PyTorch 기반 추천시스템 의존성
# 설치: pip install -r requirements_pytorch.txt

# ========== 딥러닝 프레임워크 ==========
torch>=2.1.0
torchvision>=0.16.0
torchaudio>=2.1.0
pytorch-lightning>=2.1.0

# ========== 데이터 처리 ==========
pandas>=2.0.3
numpy>=1.24.3
scikit-learn>=1.3.0
scipy>=1.11.0

# ========== 데이터베이스 ==========
sqlalchemy>=2.0.21
psycopg2-binary>=2.9.7  # PostgreSQL 드라이버
alembic>=1.12.0  # DB 마이그레이션

# ========== 웹 프레임워크 ==========
fastapi>=0.103.1
uvicorn[standard]>=0.23.2
pydantic>=2.3.0

# ========== 기존 추천시스템 라이브러리 ==========
scikit-surprise>=1.1.3  # Matrix Factorization
implicit>=0.7.2  # BPR, ALS
lightfm>=1.17  # Hybrid CF

# ========== 머신러닝 유틸리티 ==========
optuna>=3.4.0  # 하이퍼파라미터 최적화
mlflow>=2.7.1  # 실험 관리 (선택사항)
wandb>=0.15.10  # 실험 추적 (선택사항)

# ========== 개발 도구 ==========
jupyter>=1.0.0
matplotlib>=3.7.2
seaborn>=0.12.2
plotly>=5.16.1  # 대시보드용

# ========== 성능 프로파일링 ==========
memory-profiler>=0.61.0
line-profiler>=4.1.1

# ========== 유틸리티 ==========
tqdm>=4.66.1  # 진행률 표시
python-dotenv>=1.0.0  # 환경변수 관리
requests>=2.31.0
python-multipart>=0.0.6  # FastAPI 파일 업로드

# ========== CORS 처리 ==========
python-cors>=1.7.0

# ========== 로깅 ==========
loguru>=0.7.2  # 고급 로깅

# ========== 텍스트 처리 (리뷰 분석용) ==========
transformers>=4.33.2  # BERT 등
sentence-transformers>=2.2.2  # 임베딩

# ========== 이미지 처리 (차량 이미지 분석용) ==========
pillow>=10.0.0
opencv-python>=4.8.1.78

# ========== 캐싱 ==========
redis>=4.6.0  # Redis 클라이언트
hiredis>=2.2.3  # Redis 성능 향상

# ========== 테스트 ==========
pytest>=7.4.2
pytest-asyncio>=0.21.1
httpx>=0.24.1  # FastAPI 테스트용

# ========== 코드 품질 ==========
black>=23.7.0  # 코드 포매터
isort>=5.12.0  # import 정리
flake8>=6.1.0  # 린터

# ========== 타입 힌트 ==========
mypy>=1.5.1

# ========== 보안 ==========
cryptography>=41.0.4
python-jose[cryptography]>=3.3.0  # JWT

# ========== 모니터링 ==========
prometheus-client>=0.17.1  # 메트릭 수집
psutil>=5.9.5  # 시스템 모니터링

# ========== 배포 ==========
gunicorn>=21.2.0  # WSGI 서버
docker>=6.1.3  # Docker 관리

# ========== 데이터 검증 ==========
cerberus>=1.3.5  # 스키마 검증
marshmallow>=3.20.1  # 직렬화

# ========== 시각화 (대시보드) ==========
streamlit>=1.26.0  # 빠른 대시보드 (선택사항)
dash>=2.14.1  # 고급 대시보드 (선택사항)

# ========== 외부 API ==========
aiohttp>=3.8.5  # 비동기 HTTP 클라이언트
httpx>=0.24.1  # 동기/비동기 HTTP 클라이언트

# ========== 지리 정보 처리 ==========
geopy>=2.4.0  # 위치 기반 추천용

# ========== 통계 분석 ==========
statsmodels>=0.14.0